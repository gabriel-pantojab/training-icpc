import { SortProblemsDatePipe } from './sort-problems-date.pipe';

describe('SortProblemsDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortProblemsDatePipe();
    expect(pipe).toBeTruthy();
  });
});
