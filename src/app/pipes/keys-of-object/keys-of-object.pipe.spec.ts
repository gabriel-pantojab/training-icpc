import { KeysOfObjectPipe } from './keys-of-object.pipe';

describe('KeysOfObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new KeysOfObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
